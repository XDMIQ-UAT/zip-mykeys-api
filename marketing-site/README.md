# MyKeys.zip Marketing Site

Warm, friendly marketing site for MyKeys.zip built with Vite + React Router.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Builds to `../public` directory (served by the Express API).

## Deployment

The marketing site is built and deployed alongside the API to Vercel.





